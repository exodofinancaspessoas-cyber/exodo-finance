export const APP_VERSION = '1.6.0'; // Increment this on every deploy
export const DEPLOY_DATE = '2026-02-25 17:35';

export const VersionInfo = {
    version: APP_VERSION,
    date: DEPLOY_DATE,
    environment: import.meta.env.MODE,
};
